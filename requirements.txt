langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-community>=0.4,<1.0
langchain-text-splitters>=1.0,<2.0
langchain-huggingface>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-pinecone>=0.2,<1.0
# The local vector store, for VECTOR_STORE=chroma. Installed either way: the
# code imports it when a local store is opened, and pinning it here keeps the
# two stores on the same versions everywhere.
langchain-chroma>=1.1,<2.0
langgraph>=1.0,<2.0
# The checkpointer a conversation survives a restart in. In memory is the one
# langgraph carries itself, and what the console goes on using; this is the one
# that writes to a file.
langgraph-checkpoint-sqlite>=3.0,<4.0
pinecone>=7.0,<8.0
sentence-transformers>=5.0,<6.0
torch>=2.0
# The reader. PyMuPDF gives a page's text together with the rectangle each run of
# it covers, and can tell a heading from a body line and a table from a
# paragraph — none of which a line-based reader can say. It is the only thing
# that reads a PDF here: pypdf and langchain-community were what `PyPDFLoader`
# needed, and there is no loader any more.
pymupdf>=1.26,<2.0
python-dotenv>=1.0,<2.0

# The web interface: the app and the server that runs it. The stream is written
# by hand — `text/event-stream` is a few lines of text — so there is no third
# package for it.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
